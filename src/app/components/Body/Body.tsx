import React from "react";

export default function Body() {
    return (
        <main
            className="flex-1 p-6 gap-2 text-xs font-semibold">
            corpo do app
        </main>
    );
}